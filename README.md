# PlaywrightFrameworkWithTypeScript# TutorialsNinja (Qafox OpenCart) - Playwright + TypeScript

## Setup
```bash
npm i
npm run pw:install


Ctrl+Shift+P → Developer: Reload Window
Ctrl+Shift+P → Testing: Focus on Test Explorer View

npx playwright test --headed

Open playwright.config.ts:
use: {
  headless: false,
}